#include<stdio.h>
int main(){
	printf("Hello World!\n");
	int arr[] = {1, 2, 3, 4, 5};
	int len = sizeof(arr)/sizeof(a[0]);
	int sum = 0;
	for(int i = 0; i < len; i++) sum += arr[i];
	printf("The sum of the array is: %d", sum);
	return 0;


}
